int i=99,j;   // Criação das variáveis.
int *p;       // Criação do ponteiro.
p = &i;       // Ponteiro recebe o endereço de memória de i.
j = *p + 100; // j recebe apontado de p + 100, ou seja, 99 + 100 = 199.